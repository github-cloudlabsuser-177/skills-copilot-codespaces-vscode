function calculateNumbers(var1, var2) {
    // Perform the desired calculation
    const result = var1 + var2;

    // Return the calculated result
    return result;
}

function multiplyThreeNumbers(var1, var2, var3) {
    // Perform the desired calculation
    const result = var1 * var2 * var3;

    // Return the calculated result
    return result;
}
